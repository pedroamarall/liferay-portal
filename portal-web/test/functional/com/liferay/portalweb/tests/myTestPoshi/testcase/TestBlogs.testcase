@component-name = "portal-web-forms-and-data-lists"
definition {

	property testray.main.component.name = "Forms";

	setUp {
		TestCase.setUpPortalInstance();

		User.firstLoginPG();

		Navigator.openURL();
	}

	tearDown {
		var testPortalInstance = PropsUtil.get("test.portal.instance");

		if (${testPortalInstance} == "true") {
			PortalInstances.tearDownCP();
		}
	}

	@description = "Can Create a Blog"
	@priority = 5
	test CanCreateBlog {
		task ("Given Go to Blogs part") {
			BlogsNavigator.openBlogsAdmin(siteURLKey = "guest");
		}

		task ("When Create a new Blog") {
			TestTraining.CreateBlogs(
				entryContent = "This is Blog Content",
				entryTitle = "This is Title");
		}
		task ("Then confirm the creation") {
			AssertElementPresent(
				key_entryTitle = "This is Title",
				key_tagName = "This is Title",
				locator1 = "BlogsEntry#TAG")
		}
	}

	@description = "Can Delete a Blog"
	@priority = 5
	test CanDeleteBlog {
		task ("Given Go to Blogs part") {
			JSONBlog.addEntry(
				entryContent = "This is Blog Content",
				entryTitle = "This is Title");

			BlogsNavigator.openBlogsAdmin(siteURLKey = "guest");
		}

		task ("When Delete the Blog contain this Title") {
			TestTraining.DeleteBlog(rowEntry = "This is Title");
		}

		task ("Then confirm the delete") {
			AssertElementNotPresent(
				key_entryTitle = "This is Title",
				key_tagName = "This is Title",
				locator1 = "BlogsEntry#TAG")
		}
	}

	@description = "Can Edit a Blog"
	@priority = 5
	test CanEditBlog {
		task ("Given Go to Blogs part") {
			JSONBlog.addEntry(
				entryContent = "This is Blog Content",
				entryTitle = "This is Title");

			BlogsNavigator.openBlogsAdmin(siteURLKey = "guest");
		}

		task ("When Edit the Blog") {
			TestTraining.EditBlogs(
				entryTitle = "This is Title",
				entryTitleEdit = "This Title is Edited");
		}

		task ("Then confirm the edit") {
			AssertElementPresent(
				key_entryTitle = "This is Title",
				key_tagName = "This is Title",
				locator1 = "BlogsEntry#TAG")
		}
		
	}

	@description = "Can Search Blogs"
	@priority = 5
	test CanSearchBlogs {
		task ("Given Go to Blogs part") {
			JSONBlog.addEntry(
				entryContent = "This is Blog Content",
				entryTitle = "This is Title");

			BlogsNavigator.openBlogsAdmin(siteURLKey = "guest");
		}

		task ("When can Search for Blogs") {
			BlogsEntry.searchCP(
				entryContent = "Blogs Content",
				entrySearchTerm = "This is ",
				entryTitle = "This is Title");
		}
		task ("Then confirm the Search") {
			AssertElementPresent(
				key_entryTitle = "This is Title",
				key_tagName = "This is Title",
				locator1 = "BlogsEntry#TAG")
		}
	}

}