@component-name = "portal-web-forms-and-data-lists"
definition {

	property testray.main.component.name = "Forms";

	setUp {
		TestCase.setUpPortalInstance();

		User.firstLoginPG();

		Navigator.openURL();
	}

	tearDown {
		var testPortalInstance = PropsUtil.get("test.portal.instance");

		if (${testPortalInstance} == "true") {
			PortalInstances.tearDownCP();
		}
	}

	@description = "Set Screen size to custom and validate values"
	@priority = 5
	test SetScreenSizeAndValidateValues {
		task ("Given Go to Simulation") {
			TestTraining.GotoSimulation();
		}

		task ("When modify the width and the height") {
			TestTraining.typeTheweidth();
			TestTraining.typeTheheight();
		}

		task ("Then confirm the the custom size is applied") {
			AssertCssValue(
				locator1 = "ControlMenuPreviewPanel#PREVIEW_DEVICE_GENERAL_IFRAME",
				locator2 = "height",
				value1 = "1920px");

			AssertCssValue(
				locator1 = "ControlMenuPreviewPanel#PREVIEW_DEVICE_GENERAL_IFRAME",
				locator2 = "width",
				value1 = "1080px");
		}


	}

	@description = "Access Portal Global Menu and validate CONFIGURATION string at Control Panel"
	@priority = 5
	test ValidateConfigurationStringAtControlPanel {
		task ("Given Go to Menu") {
			ApplicationsMenuHelper.openApplicationsMenu();
		}

		task ("When Control Panel ") {
			TestTraining.ClickControlPanel();
		}

		task ("Then check if Confirmation its exist or not") {
			AssertTextEquals.assertText(
				locator1 = "FileTest#CHECK_CONF",
				value1 = "CONFIGURATION")
			
		}
	}

}