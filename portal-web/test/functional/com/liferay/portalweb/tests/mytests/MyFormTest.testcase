@component-name = "portal-web-forms-and-data-lists"
definition {

	property testray.main.component.name = "Forms";

	setUp {
		task ("Open new Instance of Liferay") {
			TestCase.setUpPortalInstance();
		}

		task ("Login") {
			User.firstLoginPG();
		}

		task ("Navigate to the Instance") {
			Navigator.openURL();
		}
	}

	tearDown {
		User.logoutAndLoginPG(
			userLoginEmailAddress = "test@liferay.com",
			userLoginFullName = "Test Test");
	}

	@description = "Verify if Form Translation Can be added and exist"
	@priority = 5
	test CreateFormAddTranslationThenVerifyIfTranslationExist {
		task ("Given This new form I added") {
			MyFormMacro.CreateFormWithTextAndNumeric();
		}

		task ("When I add fields and Translater and I Publish") {
			MyFormMacro.AddAFormTranslationAndPublish();
		}

		task ("Then It should verify if Translation exist") {
			MyFormMacro.VerifyIfTranslationExist();
		}
	}

}