@component-name = "portal-web-forms-and-data-lists"
definition {

	property testray.main.component.name = "Forms";

	setUp {
		task ("Open new Instance of Liferay") {
			TestCase.setUpPortalInstance();
		}

		task ("Login") {
			User.firstLoginPG();
		}

		task ("Navigate to the Instance") {
			Navigator.openURL();
		}
	}

	tearDown {
		User.logoutAndLoginPG(
			userLoginEmailAddress = "test@liferay.com",
			userLoginFullName = "Test Test");
	}

	@description = "Create a New form with Translation"
	@priority = 5
	test CanCreateFormWithTranslation {
		task ("Given A Form is created with a Text field and a numeric field") {
			ApplicationsMenuHelper.openApplicationsMenu();

			FormsAdmin.addForm();

			Testex.addFieldsForm(
				fieldPositionNumber = 0,
				fieldPositionNumber2 = 1,
				fieldType1 = "Text",
				fieldType2 = "Numeric",
				fieldValue1 = "Full Name",
				fieldValue2 = "Age",
				formDescription = "form ENG",
				formName = "form ENG",
				formPageName = "Created Form EN",
				formTitleNumber = 1);
		}

		task ("And The field titles are translated to french") {
			Form.addTranslation(localizedLanguage = "French (France)");

			Testex.changeFieldsForm(
				fieldValue1 = "Nom Prenom",
				fieldValue2 = "Age",
				formDescription = "form en frc",
				formName = "form frc",
				formPageName = "Creation formulaire en frc",
				formTitleNumber = 1);
		}

		task ("And We publish the Form") {
			Form.publishForm();
		}

		task ("And Navigate to the published Form") {
			FormsAdminNavigator.gotoPublishedForm();
		}

		task ("When We fill the english form") {
			Testex.fillFormEnglish(
				fieldName1 = "Full Name",
				fieldName2 = "Age",
				fieldValue1 = "Adnan",
				fieldValue2 = 22);
		}

		task ("And We switch to french and fill the form") {
			FormPortletConfiguration.changeLanguage(
				currentLanguage = "en-US",
				newLanguage = "français-France");

			Testex.fillFormFrench(
				fieldName1 = "Nom Prenom",
				fieldName2 = "Age",
				fieldValue1 = "Adnan",
				fieldValue2 = 22);
		}

		task ("And Navigate to the Forms portlet") {
			Navigator.openURL();

			ProductMenu.gotoPortlet(
				category = "Content & Data",
				portlet = "Forms");
		}

		task ("Then View and Assert Entries") {
			Testex.CheckForm(
				rowAny = "Adnan",
				rowEntry = "Adnan",
				rowEntry2 = "Adnan");
		}
	}

}